# 各指専用MLPのモデル評価

## 評価の目的

現行版では，親指・人差し指・中指・薬指・小指それぞれについて，指が曲がっているか，伸びているかを個別のMLPで二値分類しています．

本評価では，各指専用MLPがそれぞれの指の状態をどの程度分類できるかを確認します．

なお，本評価は各指モデル単体の二値分類性能を対象としており，5つの予測結果を組み合わせた最終的な0〜5本の指本数認識精度を評価したものではありません．

## データセット

使用したデータセットは以下です．

```text
data/hand_landmarks_and_states.csv
```

総データ数は720件です．

| 項目 | データ数 |
|---|---:|
| 左手 | 363 |
| 右手 | 357 |
| 合計 | 720 |

各データには，MediaPipeで取得した21点のランドマーク座標と，各指の状態を表す5つの正解ラベルを保存しています．

各指のラベルは以下のように定義しています．

- `0`：指が曲がっている
- `1`：指が伸びている

各指のラベル数は以下のとおりです．

| 指 | 0：曲げている | 1：伸びている |
|---|---:|---:|
| 親指 | 360 | 360 |
| 人差し指 | 360 | 360 |
| 中指 | 321 | 399 |
| 薬指 | 401 | 319 |
| 小指 | 319 | 401 |

データは主に，同一人物・同一PC・同一Webカメラ環境で収集しています．

CSVには左右の手を表す`hand`列も保存していますが，確認用の情報として使用しており，MLPの入力特徴量には使用していません．

## 特徴量

MediaPipeから取得した21点のランドマークについて，手首を原点として各座標を相対座標へ変換します．

さらに，手首から中指付け根までの距離を手の大きさの基準として，各ランドマークの座標を正規化します．

```text
各ランドマークの座標
        ↓
手首を基準とした相対座標へ変換
        ↓
手首から中指付け根までの距離で正規化
```

その後，各指の判定に使用する9点のランドマークを選択します．

各ランドマークについてx，y，zの3座標を使用するため，各MLPへの入力は以下の27次元です．

```text
9ランドマーク × 3座標 = 27次元
```

5つのMLPは同じ構造を使用しますが，各指に応じて使用するランドマークを変更しています．

## モデル構造

各指専用MLPの構造は以下です．

```text
入力：27次元
        ↓
Linear（27 → 64）
        ↓
ReLU
        ↓
Linear（64 → 32）
        ↓
ReLU
        ↓
Dropout（0.2）
        ↓
Linear（32 → 1）
```

学習時には損失関数として`BCEWithLogitsLoss`を使用しています．

推論時にはモデルの出力へSigmoid関数を適用し，出力確率が0.5以上の場合を`1`（伸ばしている），0.5未満の場合を`0`（曲げている）と判定します．

## 学習・評価条件

各指について，同じ720件のデータから対象指に必要な27次元特徴量と正解ラベルを取得し，個別に学習・評価します．

主な条件は以下です．

| 項目 | 設定 |
|---|---|
| 学習データ | 80% |
| テストデータ | 20% |
| テストデータ数 | 144件 |
| バッチサイズ | 32 |
| エポック数 | 100 |
| 学習率 | 0.001 |
| 最適化手法 | Adam |
| 損失関数 | BCEWithLogitsLoss |
| 乱数シード | 42 |
| 分割方法 | 各ラベルの比率を維持する層化分割 |

学習データとテストデータの分割には`train_test_split`を使用し，`stratify`に各指の正解ラベルを指定することで，0と1の比率を維持しています．

## 評価結果

現在リポジトリに保存している5つの学習済みモデルを，同じデータセットとテスト分割条件で評価した結果は以下のとおりです．

| 指 | 正解数 / テスト数 | テスト精度 |
|---|---:|---:|
| 親指 | 137 / 144 | 95.14% |
| 人差し指 | 142 / 144 | 98.61% |
| 中指 | 143 / 144 | 99.31% |
| 薬指 | 144 / 144 | 100.00% |
| 小指 | 142 / 144 | 98.61% |

現在のテスト分割では，全ての各指専用MLPで95%以上の分類精度となりました．

ただし，これらの数値は現在のデータセットと評価方法における各指モデル単体の分類結果であり，未知の人物や異なる環境に対して同程度の性能が得られることを示すものではありません．